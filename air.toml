root = "."
tmp_dir = ".tmp"

[build]
    cmd = "go build -o ./.tmp/main ."
    entrypoint = ["./.tmp/main""]
    include_ext = ["go", "tpl", "html"]
    exclude_ext = ["tmp", "vendor"]
    env_files = [".env"]

[log]
    time = true
